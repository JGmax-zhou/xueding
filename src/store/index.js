import Vue from 'vue';
import Vuex from 'vuex';

import{getDetailInfo} from '../utils/api'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    getDetailInfo:[]
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
